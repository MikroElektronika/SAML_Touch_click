unit Click_SAML_Touch_config;

uses Click_SAML_Touch_types;

const
    _SAMLTOUCH_TIMER_LIMIT : uint32_t = 5; // 5 ticks
    _SAMLTOUCH_BUF_WARNING : uint16_t = 192; // 192 bytes activate warning
    _SAMLTOUCH_POLL_ENABLE : uint8_t = 1; // poll enabled
    _SAMLTOUCH_CALLBACK_ENABLE : uint8_t = 0; // calback disabled
    _SAMLTOUCH_UART_CFG : array[3] of uint32_t = (115200, _UART_NOPARITY, _UART_ONE_STOPBIT); 
    
end.